import * as webpack from 'webpack';
import * as path from 'path';

export const BrowserConfiguration: webpack.Configuration = {
    target: 'web',
    output: {
        path: path.join(__dirname, '../dist/'),
        filename: 'LablebSDK.min.js',
        library: 'LablebSDK',
        libraryTarget: 'umd',
        umdNamedDefine: true,
        globalObject: 'this',
    },
    plugins: [
        new webpack.DefinePlugin({
            'process.env.BUILD_TARGET': JSON.stringify('BROWSER'),
            'process.env.LABLEB_API_BASE_URL': JSON.stringify('https://api-stage.lableb.com/api/v2')
        }),
    ],
}
