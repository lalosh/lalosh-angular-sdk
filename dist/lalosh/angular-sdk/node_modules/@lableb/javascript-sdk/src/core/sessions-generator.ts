/**
 * return unique string to be used as a session id
 * 
 * @returns {string}
 */
export function generateSessionId() {

    return (((1 + Math.random()) * 0x10000) | 0).toString(16).substring(1);
}
