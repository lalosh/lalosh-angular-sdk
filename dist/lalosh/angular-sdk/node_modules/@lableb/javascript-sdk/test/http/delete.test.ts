import { NodeHttpClient } from '../../src/http/node';

describe('Test HTTP GET', () => {

    let httpClient = NodeHttpClient();

    beforeAll(() => {
        httpClient = NodeHttpClient();
    });

   

    test('Test Delete', async () => {
        await httpClient.delete({ url: 'https://jsonplaceholder.typicode.com/todos/1', });
    });


});